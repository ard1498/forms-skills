#!/usr/bin/env bash
set -e
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PLUGIN_ROOT="$(cd "$SCRIPT_DIR/../../../../../../.." && pwd)"
source "$PLUGIN_ROOT/scripts/_resolve-workspace"
cd "$FORMS_WORKSPACE"
exec node "$SCRIPT_DIR/eds_form_validator/validate.cjs" "$@"
